# API base URL — leave as /api for local dev (Vite proxy handles it)
# Point to your deployed backend for production:
# VITE_API_BASE_URL=https://your-render-app.onrender.com/api
VITE_API_BASE_URL=/api
